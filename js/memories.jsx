/* ── On this day + Year in Review ──────────────────────────────────────
   Loads before the main app script: top level must not touch app globals. */
const OnThisDay = () => null;
const WrappedBanner = () => null;
const YearInReview = () => null;
